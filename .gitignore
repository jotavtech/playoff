# Nuxt
.nuxt
.output
.data
.nitro
.cache
dist

# Dependencies
node_modules

# Vercel
.vercel

# Env — nunca commitar credenciais
.env
.env.*
!.env.example

# Logs
logs
*.log

# Misc
.DS_Store
.idea

# Coletor — saída do tsc (o padrão `dist` acima já cobre, explícito para não confundir)
collector/dist
